{
  "name": "git-identity-switcher",
  "displayName": "Git Identity Switcher",
  "publisher": "Wisdom47",
  "description": "Easily switch between Git user identities for different repositories",
  "version": "1.0.0",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": ["Other"],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:gitIdentitySwitcher.addIdentity",
    "onCommand:gitIdentitySwitcher.switchIdentity",
    "onCommand:gitIdentitySwitcher.refreshView",
    "onCommand:gitIdentitySwitcher.deleteIdentity",
    "onCommand:gitIdentitySwitcher.sshWizard",
    "onCommand:gitIdentitySwitcher.copyPublicKey"
  ],
  "main": "./extension.js",
  "contributes": {
    "configuration": {
      "title": "Git Identity Switcher",
      "properties": {
        "gitIdentitySwitcher.identities": {
          "type": "array",
          "description": "List of Git identities used by the extension",
          "default": [],
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "username": { "type": "string" },
              "email": { "type": "string" },
              "id": { "type": "number" }
            },
            "required": ["name", "username", "email"]
          }
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "gitIdentityContainer",
          "title": "Git Identity",
          "icon": "$(person)"
        }
      ]
    },
    "views": {
      "gitIdentityContainer": [
        {
          "id": "gitIdentitySwitcher",
          "name": "Identities",
          "when": "workspaceFolderCount > 0"
        },
        {
          "id": "currentIdentityView",
          "name": "Current Identity"
        }
      ]
    },
    "commands": [
      {
        "command": "gitIdentitySwitcher.switchIdentity",
        "title": "Switch Git Identity",
        "icon": "$(person)"
      },
      {
        "command": "gitIdentitySwitcher.addIdentity",
        "title": "Add Identity",
        "icon": "$(add)"
      },
      {
        "command": "gitIdentitySwitcher.refreshView",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "gitIdentitySwitcher.deleteIdentity",
        "title": "Delete Identity",
        "icon": "$(trash)"
      },
      {
        "command": "gitIdentitySwitcher.deleteIdentityFromPalette",
        "title": "Delete Identity (Command Palette)",
        "icon": "$(trash)"
      },
      {
        "command": "gitIdentitySwitcher.sshWizard",
        "title": "SSH Setup Wizard",
        "icon": "$(key)"
      },
      {
        "command": "gitIdentitySwitcher.copyPublicKey",
        "title": "Copy SSH Public Key",
        "icon": "$(copy)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "gitIdentitySwitcher.addIdentity",
          "when": "view == gitIdentitySwitcher",
          "group": "navigation"
        },
        {
          "command": "gitIdentitySwitcher.refreshView",
          "when": "view == gitIdentitySwitcher",
          "group": "navigation"
        },
        {
          "command": "gitIdentitySwitcher.sshWizard",
          "when": "view == gitIdentitySwitcher",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "gitIdentitySwitcher.switchIdentity",
          "when": "view == gitIdentitySwitcher",
          "group": "inline"
        },
        {
          "command": "gitIdentitySwitcher.deleteIdentity",
          "when": "view == gitIdentitySwitcher",
          "group": "inline"
        },
        {
          "command": "gitIdentitySwitcher.copyPublicKey",
          "when": "view == gitIdentitySwitcher",
          "group": "inline"
        }
      ]
    }
  }
}